# 后端 API 地址配置
# ⚠️ 安全架构说明：
# 前端（本地） → 国内后端 → 直连联通官方 API
#
# 开发环境：http://localhost:3000
# 生产环境：https://your-api-server.example.com
#
# 注意：后端需要实现以下代理接口：
#   POST /gettoken - 登录接口（返回 ecs_token, ecs_acc, captcha_appid）
#   POST /ocs_proxy - OCS 数据查询代理（5G 余量）
#   POST /basicdata_proxy - 基本数据查询代理（速率信息）
#   POST /qci_proxy - QCI 数据查询代理（限速信息）
#
# 敏感数据说明：
# - AppID 和 ECS_ACC 由登录时返回，存储在本地缓存
# - 登出时自动清理缓存
# - 不再使用环境变量存储敏感信息
#
VITE_MAIN_API=http://localhost:3000

